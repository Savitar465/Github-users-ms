application-version: '@project.version@'
application-description: '@project.description@'
server:
  port: '8081'
spring:
  profiles:
    active: @activatedProperties@
  application:
    name: ms-usuarios
  #  cloud:
  #    config:
  #      profile: default
  rabbitmq:
    port: '5672'
    host: rabbitmqa.usecc.com
    username: admin
    password: mC6Y*Vn2scvP
    exchange: topic-exchange
    user-queue: usuario.queue
    routing-key: "usuario.#"
    virtual-host: /
    listener:
      simple:
        missing-queues-fatal: false
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    usuarios:
      url: jdbc:postgresql://192.168.127.181:5433/usuario_database?currentSchema=usuarios_nuevo
      username: jonaso
      password: SnZz59*b8#Ff
      driver-class-name: org.postgresql.Driver
      jpa:
        properties:
          hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
    sgsidb:
      url: jdbc:postgresql://192.168.127.181:5433/sgsidb?currentSchema=pruebas_sgsi
      username: fernando
      password: W&WQt^bq3jTP
      driver-class-name: org.postgresql.Driver
      jpa:
        properties:
          hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
  #  datasource:
  #    password: SnZz59*b8#Ff
  #    username: jonaso
  ##    url: jdbc:postgresql://192.168.127.181:5433/usuario_database
  #    url: jdbc:postgresql://192.168.127.181:5433/usuario_database?currentSchema=usuarios_nuevo
  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: https://keycloakqa.apps.gob.bo/realms/test
          jwk-set-uri: https://keycloakqa.apps.gob.bo/realms/test/protocol/openid-connect/certs
      client:
        registration:
          keycloak:
            client-id: ms-usuarios-local
            client-secret: BwiYJVg29lpnaJqfdBUjvrGKa9IL06H1
            authorization-grant-type: client_credentials
            scope: openid
        provider:
          keycloak:
            issuer-uri: https://keycloakqa.apps.gob.bo/realms/test
logging:
  level:
    org:
      keycloak: DEBUG
      springframework:
        security: DEBUG


eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    registry-fetch-interval-seconds: 5
    service-url:
      defaultZone: http://eureka:password@localhost:8761/eureka

keycloak-client:
  server-url: https://keycloakqa.apps.gob.bo
  realm: test
  client: admin-java
  client-secret: O6Jxhw2VWPaXozGVG97Hwasbzyaq91UX
